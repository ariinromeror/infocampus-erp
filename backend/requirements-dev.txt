# Dependencias de desarrollo/test (no se instalan en producción/Render).
# Uso: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.28.1
