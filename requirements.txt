# ===================================================================
# InfoCampus ERP — Dependencias de scripts_db/ (seed de datos de demo)
# ===================================================================
# NO es el requirements.txt de la API. La API usa backend/requirements.txt
# (referenciado en render.yaml y en el README como fuente de verdad).
#
# Este archivo cubre únicamente lo que importa scripts_db/populate.py:
# conexión directa a Postgres (psycopg2) + generación de datos de demo
# (Faker/tqdm) + hashing de contraseñas (passlib/bcrypt) + carga de .env.
#
# Las versiones de paquetes que también usa el backend (psycopg2-binary,
# passlib, bcrypt, python-dotenv) están fijadas IGUAL que en
# backend/requirements.txt para evitar drift entre ambos entornos.
#
# Uso: pip install -r requirements.txt && python scripts_db/populate.py
# ===================================================================

psycopg2-binary==2.9.10
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
python-dotenv==1.0.1
Faker==24.0.0
tqdm==4.66.5
